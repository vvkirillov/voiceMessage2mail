Voice message -> Text -> Email-message translator.<br>
You say a phrase, the application converts it to text and sends it as an e-mail message to predefined address.<br>
Address is defined in preferences.
